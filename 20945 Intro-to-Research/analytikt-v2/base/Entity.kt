package analytikt.base

/**
 * A mathematical Entity of type TDomain is a generalisation of all mathematical objects that
 * deal with values of the domain type. Many entities can be domain sets themselves:
 * For example, The set of real number sets, the set of matrices above the real numbers
 * and the set of polynomials above the real numbers are all entities of real numbers and
 * also domain sets themselves, and lines and polygons are entities of Points inside a 2d or even 3d space.
 *
 */
interface Entity<TDomain>